<?php

$idade = 29;
echo gettype ($idade);

// No exemplo acima utilizamos o método getType para identificar que o tipo da variável $idade é integer

$salario = 1000.30;
echo gettype ($salario);

// No exemplo acima utilizamos o método getType para identificar que o tipo da variável $salario é double

$texto = "Olá mundo!";
echo gettype ($texto);

// No exemplo acima utilizamos o método getType para identificar que o tipo da variável $idade é string

$verdadeiro = true;
$falso = false;

echo gettype($verdadeiro);
echo gettype($falso);

/* No exemplo acima utilizamos o metodo getType para identificar que os tipos de
 * variáveis $verdadeiro e $falso que sãoo boolean
 */
