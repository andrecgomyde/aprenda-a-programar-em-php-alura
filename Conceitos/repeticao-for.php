<?php

for ($contador = 1; $contador <= 15; $contador ++)
    echo "#$contador" . PHP_EOL;

/*
 * No exemplo acima, na linha definimos a função for (para) e as condições abaixo
 * a variável $contador = 1
 * a variável $contador deve ser <= (menor ou igual) no número 15
 * a variável contador sempre deve somar +1 até chegar no número 15
 */
